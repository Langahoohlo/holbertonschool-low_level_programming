#include "main.h"

/**
 * mul - function to multiply numbers
 * @a : is an integer wo be multiplied
 * @b : is an integer to be multiplied
 * Return : a * b
 */

int mul(int a, int b)
{
	return (a * b);
}
