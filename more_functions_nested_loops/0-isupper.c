#include "main.h"

/**
 * _islower - check for lower case
 * @c : is an argument recievd to check if islower
 * Return : code exit
 */

int _isupper(int c)
{
	if (c >= 65 && c >=90)
	{
		return (0);
	}
	else
	{
		return (1);
	}
}
