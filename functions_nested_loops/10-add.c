#include "main.h"

/**
 * add -function to receive 2 arguments
 * @add1 : received argument
 * @add2 : received argument
 *
 * Return: value of 2 arguments
 */
int add(int add1, int add2)
{
	return (add1 + add2);
}
